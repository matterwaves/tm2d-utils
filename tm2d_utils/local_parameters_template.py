"""Local defaults template for tm2d-utils scripts.

Copy this file to ``tm2d_utils/local_parameters.py`` in your checkout and set
machine-specific paths there. ``local_parameters.py`` is git-ignored so private
paths do not get committed.
"""

DEFAULT_WORKSPACE_ROOT = None
DEFAULT_WORKSPACE_ROOT_IS_REMOTE = True
DEFAULT_PDB_FPATH = None
DEFAULT_DENSITY_HELPER_FPATHS = ()
